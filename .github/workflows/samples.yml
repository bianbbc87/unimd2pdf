name: Generate Sample PDFs

on:
  push:
    branches: [main]
    paths:
      - "samples/**"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - name: Generate GitHub App token
        id: app-token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}

      - uses: actions/checkout@v4
        with:
          token: ${{ steps.app-token.outputs.token }}

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.23"

      - name: Install Chrome
        uses: browser-actions/setup-chrome@v1
        with:
          chrome-version: stable

      - name: Install mmdc
        run: npm install -g @mermaid-js/mermaid-cli

      - name: Create mmdc puppeteer config
        run: |
          echo '{ "args": ["--no-sandbox", "--disable-setuid-sandbox"] }' > /tmp/puppeteer.json

      - name: Build unimd2pdf
        run: go build -o unimd2pdf ./main.go

      - name: Generate sample PDFs
        env:
          MMDC_PUPPETEER_CONFIG: /tmp/puppeteer.json
        run: |
          cd samples
          for f in *.md; do
            echo "Converting: $f"
            if [ "$f" = "08-dark-theme.md" ]; then
              ../unimd2pdf -i "$f" --theme dark || true
            else
              ../unimd2pdf -i "$f" || true
            fi
          done

      - name: Commit and push PDFs
        run: |
          git config user.name "unimd2pdf-bot[bot]"
          git config user.email "unimd2pdf-bot[bot]@users.noreply.github.com"
          git add samples/*.pdf
          git diff --staged --quiet || git commit -m "chore: regenerate sample PDFs [skip ci]"
          git push
